>>>probeersel, gevolgd via website mongodb

const credentials = require("./Credentials.js");

DB_URI=mongodb+srv://${credentials.username}:${credentials.password}@web2.qwg6m.mongodb.net/?retryWrites=true&w=majority&appName=Web2
